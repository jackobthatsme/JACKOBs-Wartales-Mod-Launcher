name: Build Windows Launcher

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore JACKOBsWartalesModLauncher.csproj

      - name: Build
        run: dotnet build JACKOBsWartalesModLauncher.csproj -c Release --no-restore

      - name: Publish self-contained win-x64
        run: >-
          dotnet publish JACKOBsWartalesModLauncher.csproj
          -c Release
          -r win-x64
          --self-contained true
          -p:PublishSingleFile=true
          -p:DebugType=None
          -p:DebugSymbols=false
          -o artifacts/win-x64

      - name: Upload launcher artifact
        uses: actions/upload-artifact@v4
        with:
          name: JACKOBs-Wartales-Mod-Launcher-v0.3.0-win-x64
          path: artifacts/win-x64/
          if-no-files-found: error
